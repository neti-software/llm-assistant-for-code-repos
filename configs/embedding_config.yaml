# embedding.yaml

text:
  type: local              # or "cloud"
  model_name: BAAI/bge-large-en-v1.5

  # --- Local options ---
  # cache_dir: ./cache/text
  # max_length: 8192
  # device: cuda

  # --- Cloud options ---
  # provider: openai        # or "cohere", "together", "mcp"
  # endpoint: http://localhost:8000/embeddings   # for MCP server
  # api_key: sk-xxx

code:
  type: local              # or "cloud"
  model_name: jinaai/jina-embeddings-v2-base-code

  # --- Local options ---
  # cache_dir: ./cache/code
  # max_length: 2048
  # device: cuda

  # --- Cloud options ---
  # provider: openai
  # endpoint: http://localhost:8000/embeddings
  # api_key: sk-xxx
